**To build and run the C++ version:**

Open a powershell command prompt in the root directory and run the following commands:

cd cpp

.\run.ps1

**To build and run the C# version:**

Open a powershell command prompt in the root directory and run the following commands:

cd csharp

.\run.ps1
